import React, { useState, useEffect, useRef } from "react";
import { Square } from "lucide-react";
import { useEditorStore, useImageStore } from "@/store/store";

interface HandlerPosition {
  x: number;
  y: number;
}

const CropHandlers = () => {
  return <>hi</>;
};

export default CropHandlers;
